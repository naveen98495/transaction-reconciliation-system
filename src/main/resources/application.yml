spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reconciliation_db
    username: root
    password: "Naveen@123"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9091
